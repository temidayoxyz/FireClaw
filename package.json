{
  "name": "fireclaw-gateway",
  "version": "1.0.0",
  "description": "OpenClaw Gateway Deployment",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "npm install && npm run build"
  },
  "dependencies": {
    "@openclaw/whatsapp": "latest",
    "express": "^4.18.2",
    "dotenv": "^16.0.0"
  }
}
